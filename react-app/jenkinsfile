pipeline{
    stages{
        trigger{
            cron('H/2 * * * *')
        }
        stage("Clone Repository"){
            steps{
                git branch:"main"
                url :"https://github.com/sunnymca107/react-app.git"
            }
        }
        Stage("Install Ract"){
            steps{
                sh"npm install"
                sh "npm vite@latest react-app"
            }
        }
    }
}
